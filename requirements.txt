# Core libraries
pandas>=1.3.0
shapely>=2.0.0
geopandas>=0.13.0

# Required for geopandas to handle shapefiles and projections
fiona>=1.9.0
pyproj>=3.4.0
rtree>=1.0.1

# Optional (recommended for spatial indexing and better performance)
numpy>=1.21.0

# Suppress warnings
warnings  # Built-in; not needed to install, included for reference
